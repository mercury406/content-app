import 'package:flutter/material.dart';

class HoroscopeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Гороскопы"),),
      body: Center(
        child: Text("Гороскопы"),
      ),
    );
  }
}
