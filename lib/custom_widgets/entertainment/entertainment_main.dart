import 'package:flutter/material.dart';

class EntertainmentScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Развлечения"),),
      body: Center(
        child: Text("Развлечение"),
      ),
    );
  }
}
